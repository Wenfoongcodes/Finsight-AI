# FinSight AI — Dashboard Space requirements
# This file is used by HuggingFace Streamlit Spaces directly.
# It must be a flat requirements.txt at the repository root.
# ML training deps are excluded — the dashboard only calls the API.

# ── Web / HTTP ─────────────────────────────────────────────────────────────
requests==2.32.3
httpx==0.27.0

# ── Streaming (SSE consumer for /predict/stream and /agent/stream) ─────────
sseclient-py>=1.8.0

# ── Frontend ───────────────────────────────────────────────────────────────
streamlit>=1.36.0
plotly==5.22.0

# ── Market data (for the price sparkline chart in the Market Data tab) ─────
yfinance==0.2.40
pandas==2.2.2
numpy==1.26.4

# ── Config ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1